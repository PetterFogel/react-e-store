import { makeStyles } from "@mui/styles";

export const loaderStyle = makeStyles({
  root: {
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
    width: "100%",
    height: "20rem"
  }
});
